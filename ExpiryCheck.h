#pragma once
#include"Array.h"

void print_date(int* month,int* year);

void check_for_expired_meds( controller* array_control,int month,int year);

void give_stock_status(controller* array_control);

void show_med_withType(controller* array_control);
